spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.luma: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
