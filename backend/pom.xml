<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>com.luma</groupId>
  <artifactId>luma-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>luma-backend</name>
  <description>LUMA - API de finanzas personales</description>

  <properties>
    <java.version>25</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <springdoc.version>3.1.1</springdoc.version>
    <!--
      1.3.0 no lee bytecode de Java 25: su ASM falla con "Unsupported class file
      major version 69" en CADA clase del proyecto. Como todas las reglas llevan
      allowEmptyShould(true), pasaban sin evaluar nada. Verdes y vacias.
      Java 25 entra en 1.4.1; la 1.4.2 llega hasta Java 26.
    -->
    <archunit.version>1.4.2</archunit.version>
  </properties>

  <dependencies>
    <!-- Web -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Persistencia -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <!--
      Spring Boot 4 modularizo las autoconfiguraciones: cada tecnologia vive en
      su propio modulo `spring-boot-<tecnologia>`. Con solo `flyway-core` la
      libreria queda en el classpath pero NADIE la arranca, y las migraciones
      no corren. El starter es lo que trae la autoconfiguracion.
      flyway-mysql sigue aparte: es el modulo de dialecto de MySQL.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-mysql</artifactId>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- Seguridad -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <!-- Emision y validacion de JWT con el soporte nativo de Spring Security,
         en lugar de un filtro propio. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>

    <!-- Correo. El starter, no solo la libreria: en Spring Boot 4 la
         autoconfiguracion vive en el modulo del starter. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>

    <!-- Observabilidad -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- Documentacion de API -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <!-- Tests -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!--
      Testcontainers 2.x renombro sus artefactos y reacomodo sus clases: el
      modulo `mysql` es ahora `testcontainers-mysql`, y MySQLContainer se movio
      de org.testcontainers.containers a org.testcontainers.mysql.

      No se declara <version>: la aporta el BOM de Testcontainers que importa
      spring-boot-dependencies. Si Maven reclama que falta, hay que importar
      testcontainers-bom en dependencyManagement.

      No se usa el modulo de JUnit Jupiter a proposito. El contenedor se arranca
      una sola vez por JVM con el patron de contenedor compartido: es lo que
      recomienda Testcontainers cuando varias clases prueban contra la misma
      base, y evita levantar un MySQL por clase.
    -->
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-mysql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
