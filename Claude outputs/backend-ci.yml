name: Backend CI

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'
  pull_request:
    paths:
      - 'backend/**'
      - '.github/workflows/backend-ci.yml'

concurrency:
  group: backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Compilar y probar
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Configurar JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven

      - name: Compilar
        run: mvn -B --no-transfer-progress clean compile

      - name: Tests
        # Los tests de integracion usan Testcontainers; el runner de GitHub
        # ya trae Docker en ejecucion.
        run: mvn -B --no-transfer-progress verify

      - name: Verificar que el esquema se reconstruye desde cero
        # Riesgo R7: la base debe poder recrearse solo con las migraciones.
        # Si una migracion deja de ser reproducible, se detecta aqui.
        run: mvn -B --no-transfer-progress test -Dtest=LumaApplicationTests

      - name: Publicar resultados
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: backend/target/surefire-reports/
          if-no-files-found: ignore
